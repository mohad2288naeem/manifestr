import { ChevronDown as ChevronDownIcon } from 'lucide-react'

export default function ChevronDown({ className = '' }) {
  return (
    <ChevronDownIcon className={className} size={16} strokeWidth={1.5} />
  )
}
